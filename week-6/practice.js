/* In class learning */

// Map creates a new array but doesn't affect the previous array
/*
 const numbersDoubled = numbers.map((num)=>{
    return num * 2;
 }
);

const users = [
    {name:"Alice", age: 25, active:false},
    {name:"Bob", age: 32, active:true},
    {name:"Charlie", age: 25, active:false},
    {name:"David", age: 25, active:true}
]

// Filter: Usually returns all things that are true 

const evenNumbers = numbers.filter( (num) =>{
    return num % 2 === 0;
})
console.log(evenNumbers);
const activeUsers = users.filter( (user) =>{
    return user.active;
});
console.log("Active Users:", activeUsers);

const inactiveUsers = users.filter( (user) =>{
    return !user.active;
});
console.log("Active Users:", inactiveUsers);

//For Each practice
let sum = 0;
numbers.forEach((num,index) => {
    sum+=num;
    console.log(`[${index}] = ${num}`)
});
console.log(`The sum of the numbers is ${sum}.`) 


//The second parameter is the total because reduce keeps the sum of the numbers
const numbers = [1, 2, 3, 4, 5];
const sum = numbers.reduce( (num, total) =>{
    return total + num;
})
console.log(num);
*/
